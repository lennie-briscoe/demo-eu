<div class="mobile-nav-menu">

    {% for link in navigation.linksMiddle.all() %}

        <a class="link" href="{{ link.url }}">
            <span class="link-text">
                {{ link.title }}
            </span>
        </a>

    {% endfor %}

    {% for link in navigation.linksRight.all() %}

        <a class="link" href="{{ link.url }}">
            <span class="link-text">
                {{ link.title }}
            </span>
        </a>

    {% endfor %}

</div>
