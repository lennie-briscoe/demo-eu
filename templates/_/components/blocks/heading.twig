{# @var craft \craft\web\twig\variables\CraftVariable #}

{% set heading = theBlock.heading %}

{% if heading %}

    <div class="content-block heading-block" data-scroll-section>
        <div class="heading">
            {{ heading | typogrify }}
        </div>
    </div>

{% endif %}
